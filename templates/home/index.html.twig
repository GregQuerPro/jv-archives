{% extends 'base.html.twig' %}

{% block title %}JV Archives - L'archives des JV{% endblock %}

{% block body %}
<h1>Accueil</h1>

    <div>
        {% for article in articles %}
            <div>
                <img src="{{ vich_uploader_asset(article, 'imageFile') }}" alt="{{ article.title }}">
            </div>
        {% endfor %}
    </div>
{% endblock %}
